item do
  expected do
    url_xpath
    url 'http://cgit.freedesktop.org/p11-glue/p11-kit/refs/tags'
    xpath '//*[@id="cgit"]/div[1]/table/tr[2]/td[1]/a'
    regex /^([\d.]+)$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end

  deps do
    item do
      name 'libtasn1'

      expected do
        github_release
        repo "amylum/libtasn1"
      end

      actual do
        grep
        file 'Makefile'
        regex /^LIBTASN1_VERSION = ([\d.-]+)$/
      end
    end
  end
end
